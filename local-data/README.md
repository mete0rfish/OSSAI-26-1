# 실습 데이터 폴더

AIHub `멀티모달 정보검색 데이터_Sample`의 내용물을 `aihub/source/`에 넣는다.

```text
local-data/aihub/
├── source/       다운로드한 AIHub 파일
├── prepared/     PNG, API용 JPEG, page text와 manifest
└── cases.jsonl   workflow가 읽는 질문 40건
```

`prepared/`와 `cases.jsonl`은 실습 명령을 실행하면 만들어진다.
